# 30 · v1.2 终版商用优化自测报告

- 日期：2026-09-07
- 执行 Agent：Codex
- 验收契约：docs/29 §5（V1–V8）、docs/28 §8（T31–T39）
- 基线：`main @ b86e326`（155 测试全绿），本版为 additive + C2 默认 dominant 修订

## 命令与测试

| 项 | 结果 |
|---|---|
| `npm test` | ✅ 169 例全绿（既有 155 + 新增 14） |
| `npm run build` | ✅ `tsc --noEmit` + Vite 构建成功 |
| 既有 golden | ✅ solid-red / yellow-gradient / post 管线 hash 均未变化 |

## 改动清单

| 编号 | 落点 | 内容 |
|---|---|---|
| E1 | `src/core/pipeline/downsample.ts` | 新增 `downsampleDominantV2`：Lab 10×12×12 感知桶、空桶 0.5 平滑、稀疏区 <4px 回退 average；`downsample()` dominant 分支改调 V2，旧 `downsampleDominant` 保留 |
| E2 | `src/core/pipeline/chromaFeatures.ts` + `features.ts` | 新增彩度小特征通道 `classifyChromaFeature`（hue 25°/dC 0.12/C 0.10/成簇 3），特征簇均值回采 |
| E3 | `src/core/post/merge.ts` + `pipeline/index.ts` | 新增 `mergeAdjacent`（ΔE≤6、3..40 格）；dominant+features 且无后处理路径自动接入，置于 speckle 前 |
| E4 | `features.ts` | 新增 `softEdgeArbitrate`（SOFT_MARGIN=2），对 0.03..0.15 低对比块做邻域归属仲裁，不写 protect |
| E5 | `src/core/color/ciede2000.ts` + `match.ts` | 新增 `ciede2000Weighted`；`C*<25` 时走 `{kL:0.85,kC:1.05,kH:1.15}` 加权匹配 |
| C2 | `modePresets.ts` + `state.tsx` | photo/cartoon 与冷启动默认降采样改为 `dominant` |

## 验收矩阵

| # | 验收点 | 结果 |
|---|---|---|
| V1 | 默认照片 + photo 预设 `protectFeatures=true` + mode=dominant | ✅ |
| V2 | 「自定义」下拉与手改联动 | ✅ 既有 5 例不回归 |
| V3 | 底板 5 档 32/52/78/104/120 | ✅ |
| V4 | 限色 chips 24…313 | ✅ |
| V5 | E1–E5 单测 + golden 不回归 | ✅ |
| V6 | 120×120 全管线 ≤2s | ✅ 兔子 1104×1104 实测 1017.9ms |
| V7 | 兔子/卡通 A/B 特征与色块 | ✅ 色数与孤立小块均下降，截图见 `.tools/v12final-shots/` |
| V8 | 155 + 新增 ≥14 全绿 | ✅ 169 全绿 |

## 新增测试

- `tests/unit/downsample-dominant-v2.test.ts`：确定性、稀疏回退、dominant 主导。
- `tests/unit/post-merge.test.ts`：同色系并入、ΔE 异色保留、protect/空格安全、幂等。
- `tests/unit/post-features-chroma.test.ts`：低饱和粉点保护、低对比柔边仲裁。
- `tests/unit/color-weighted.test.ts`：加权距离等价/浅色排序。
- `tests/unit/v12-final-pipeline.test.ts`：dominant+merge 管线与确定性。
- `modePresets/ui-settings`：C2 默认 dominant 断言。

## 效果数据（同图 A/B，长边 78，无后处理照片路径）

| 样例 | 路径 | 色数 | ≤2 格孤立小块 | 说明 |
|---|---|---:|---:|---|
| 兔子 1104×1424 → 60×78 | average（off） | 40 | 377 | 默认 average + features |
| 兔子 1104×1424 → 60×78 | final dominant+E2-E5 | 34 | 290 | 色数 -15%，小块 -23% |
| 卡通 609×814 → 58×78 | average（off） | 33 | 583 | |
| 卡通 609×814 → 58×78 | final | 27 | 267 | 色数 -18%，小块 -54% |

截图：`.tools/v12final-shots/rabbit-off.png`、`rabbit-on.png`、`cartoon-off.png`、`cartoon-on.png`。

## 性能测录（120×120 方形板，兔子 1104×1104 中心裁方）

| 阶段 | 实测 |
|---|---:|
| E1 采样 + E2/E4 特征 | 781.2ms |
| E5 加权匹配 | 230.4ms |
| E3 合并 | 6.3ms |
| 合计 | 1017.9ms（≤2s 达标） |

测录口径为 worker 内采样/匹配/合并阶段，不含首帧渲染；源 1.2M 像素、14400 格。

## Golden 与语义修订

- 本版既有 golden hash 全部保持：solid-red `8937b118…`、yellow-gradient `f005e3d3…`、post 管线无变化。
- docs/01 §3.1、docs/03 §2.1/§9.5 已登记 v1.2-final 默认 dominant 修订；docs/25 C0–C3 决策（自定义保留、底板 5 档、限色档位）维持现状不重做。

## 遗留交 Claude 决议

- regionClean 仍为 opt-in 默认关；outline 连续性微调未接入默认，维持 docs/27 §6.2 决议。
- 兔子原图视觉上的瞳孔/耳尖粉特征需人工放大复核；本版提供 `.tools/v12final-shots/` 对比截图与确定性 hash 支撑。
