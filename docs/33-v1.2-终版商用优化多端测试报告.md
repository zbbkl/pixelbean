# 33 · v1.2 终版商用优化 · 多端测试报告（Claude Code 复核）

- 日期：2026-09-07
- 执行 Agent：Claude Code（质量验收负责人 · 多端兼容）
- 验收契约：商用级标准（移动端布局正常 / 桌面全功能 / 多浏览器兼容）；`docs/29` §5 A 组遗留 + 本版 C2 默认 dominant
- 环境：Chrome（`C:/Program Files/Google/Chrome/Application/chrome.exe`）headless + `playwright-core`（临时安装、跑后卸载）；Vite dev `:5173`；真实页面（非静态截图）
- 结论：**PASS**（六档视口 0 失败；默认主导色生效；移动/平板/桌面版式正确；0 控制台/页错误）

---

## 1. 测试范围与方法

本版**无布局改动**（仅默认降采样模式 `average→dominant` 的**状态默认值**变化，纯 JS 无 CSS）。因此重点验证：新默认在真实浏览器各视口正确渲染、且未回归 v1.2.1 已验证的布局不变量。

自写审计 `.tools/v12final-shots/audit-final.mjs`（playwright-core 驱动安装的系统 Chrome，冷启动清空 localStorage 后重载，等待 canvas + `source-foot` 出现，逐视口断言）：

- 每个视口：无横向溢出、0 console/pageerror、无「取消」按钮、冷启动默认「主导色」active 且「面积平均」非 active。
- 移动（≤767px）额外断言底栏不变量：`.app-body` 底边 == `.mobile-nav` 顶边（差≤1.5px）、nav 贴合视口底部、无子元素越过 nav。

## 2. 结果（六档视口）

| 视口 | 版式 | 无溢出 | 0 报错 | 默认主导色 | 无取消 | 底栏不变量 |
|---|---|---|---|---|---|---|
| 320×568 | 手机 | ✅ | ✅ | ✅ | ✅ | ✅（底边 510 == nav 顶 510） |
| 360×800 | 手机 | ✅ | ✅ | ✅ | ✅ | ✅（742 == 742） |
| 390×844 | 手机 | ✅ | ✅ | ✅ | ✅ | ✅（786 == 786） |
| 768×1024 | **平板（双栏，无底栏）** | ✅ | ✅ | ✅ | ✅ | N/A（无 `.mobile-nav`） |
| 1024×768 | 桌面 | ✅ | ✅ | ✅ | ✅ | N/A |
| 1440×900 | 桌面 | ✅ | ✅ | ✅ | ✅ | N/A |

`final-results.json` → `failed: 0`；各视口截图 `.tools/v12final-shots/*-final.png`。

> 说明：768px 落在 `@media(max-width:767px)` 之上，平板采用**双栏版式**（左参数 + 右统计，无底部三段导航），属设计内四档断点（docs/14），非缺陷；故底栏不变量在此不适用。

## 3. 关键配置生效核验

- **默认降采样 = 主导色**：六档视口冷启动均断言「主导色」按钮 `active`（`settings.mode==='dominant'`），`面积平均` 非 active。✓（对应 C2 修订）
- **目标模式 = 照片**：平板/桌面截图显示「0 目标模式」下拉=照片。✓
- **无「取消」按钮**：六档视口断言 count=0。✓
- **色板 / 限色档位**：平板截图见「智能限色 K」chips 24/48/72/96/120/144/168/221/313。✓（C3 维持）
- 底板 5 档 / 自定义下拉语义：沿用 v1.2.1，未改动（单元回归绿）。

## 4. 桌面全功能抽查

- 桌面 1024/1440：右栏统计/用量表完整渲染（色号、数量、占比、勾选），导出按钮（PNG/CSV/打印）在位；无溢出、0 报错。
- 转换在工作线程（worker），主线程不阻塞；加载样例 → 出图 → `网格 58×46·2668格` 文案出现（等待条件达成）。✓

## 5. 多浏览器兼容

- 本版新增代码全部为**纯函数算法**（桶化/聚类/连通域/加权距离），仅依赖标准 ES、Canvas、`Int16Array`/`Float64Array` 等既有的通用能力，无浏览器专有 API、无 CSS 依赖、无 Worker 协议改动。
- 因此兼容性随既有基线：现代 Chrome/Edge/Firefox/Safari（docs/01 §7）。本轮以 Chromium（Chrome）实测通过；Safari/Firefox 因无本机引擎，依据“零专有 API + 既有兼容契约”推断兼容，未逐引擎实测——列为人工补充项。

## 6. 结论

- **PASS**。移动端（320/360/390）布局正常、底栏不遮挡；平板 768 双栏正确；桌面全功能；默认「主导色」/「照片」/无取消/限色档位全部生效；0 控制台/页错误。
- **人工补充建议**：Firefox/Safari 引擎实测（本机仅有 Chrome）；`docs/30` 与 `docs/32` 指出的真实角色图高分辨率特征目检。
