# 36 · v1.2 P0-1 修复自测报告

- 日期：2026-09-08
- 执行 Agent：Codex
- 验收契约：docs/35 §1/§2（P0-1 唯一改动：A+B 两处字面修复）

## 改动 diff

`src/core/pipeline/downsample.ts` 只改动两处：

```diff
-const LAB_NEIGHBOR_WEIGHT = [1, 0.5, 0.5] as const;
+const LAB_NEIGHBOR_WEIGHT = [0.5, 0.5, 0.5] as const;

                 const weight =
-                  (dl === 0 ? LAB_NEIGHBOR_WEIGHT[0] : 1 - LAB_NEIGHBOR_WEIGHT[0]) *
-                  (da === 0 ? LAB_NEIGHBOR_WEIGHT[1] : 1 - LAB_NEIGHBOR_WEIGHT[1]) *
-                  (db === 0 ? LAB_NEIGHBOR_WEIGHT[2] : 1 - LAB_NEIGHBOR_WEIGHT[2]);
+                  (dl === 0 ? 1 : LAB_NEIGHBOR_WEIGHT[0]) *
+                  (da === 0 ? 1 : LAB_NEIGHBOR_WEIGHT[1]) *
+                  (db === 0 ? 1 : LAB_NEIGHBOR_WEIGHT[2]);
```

未改 `emptySmooth`、`LAB_BUCKETS_*`、`labBucket`、`n<4` 回退、返回结构或 `downsample()` 签名。

## Harness before/after

运行路径：`npx tsc -p .tools/bench/tsconfig.json` → `node .tools/bench/out/.tools/bench/_bench.js`。

| 指标 | before | after |
|---|---:|---:|
| T1_photo `maxDE` | 44.69 | 36.81 |
| T1_photo `distinctL` | 7 | 7 |
| T1_photo `reversals` | 7 | 8 |
| T1_cartoon `maxDE` | 100.00 | 100.00 |
| T3_photo20 `preserved / total` | 0 / 8 | 0 / 8 |
| T3_photo20 `colors` | 3 | 3 |

结论（如实记录）：

- 两处权重修复已按任务书生效，L 轴邻居不再恒为 0；`maxDE` 从 44.69 降至 36.81（-17.6%），说明亮度方向邻桶借量确实被引入。
- `distinctL` 仍为 7；`reversals` 为 7→8，未达到“趋近 0”的预期。P0-1 的剩余带纹表现说明本组合成样本中还受 dominant 桶化主导项/后处理影响，未在任务书红线内进一步改动。
- T3 保持 0/8，与任务边界一致（P0-3 单列，不在此任务解决）。

## 回归

| 项 | 结果 |
|---|---|
| `npx tsc --noEmit` | ✅ |
| `npm test` | ✅ 169 例全绿 |
| `npm run build` | ✅ |
| 既有 golden / fixture | ✅ 无变化 |

## 确定性

harness 与单元测试均为纯函数、无随机源；同输入逐格结果一致（`v12-final-pipeline.test.ts` 已覆盖全管线双跑）。

## 待 Claude 复核

- 仅确认 `downsample.ts` A+B 两处 diff。
- 若验收要求 `reversals` 也显著下降，需按 docs/34 后续方案另行立项；P0-1 本身在两处字面改动范围内未再做越界修改。
