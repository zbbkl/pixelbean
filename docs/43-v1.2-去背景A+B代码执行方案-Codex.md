# 43 · Codex 执行代码方案（去背景 A + B）

> 用途：**可直接执行**的代码级方案。DeepSeek Harness（规格）→ **Codex（按本文实现）** → Claude Code（复核）。
> 承接：[`docs/41`](41-v1.2-去背景A+B实现任务书-Codex.md)（A+B 契约）、[`docs/42`](42-去背景与图像转化开源调研借鉴笔记.md)（算法借鉴）、[`docs/39`](39-v1.2-去背景验收报告-ClaudeCode.md)（不通过根因）。
> 基线：工作区未提交实现（`HEAD=1c02089`）。**本方案是对现有未提交实现的精确修订**，不是重写。
> 红线：任何情况下**默认开启不得吞主体/产空图纸**；仅用确定性算法 + 纯前端零依赖。

---

## 0. 执行概览

| 阶段 | 内容 | 文件 | 门禁 |
|---|---|---|---|
| **A** | 默认关闭 + `reliable` 兜底 | `state.tsx` `modePresets.ts` `subject.ts` `pipeline/index.ts` | 关闭态逐格=上一版；不可信必回退 |
| **B-1** | 多尺度结构梯度屏障 | `subject.ts` | G1–G4 |
| **B-2** | Guided filter 掩码精修 | `subject.ts` | G1–G4 |
| **B-4** | 边缘去色污染（白边） | `subject.ts` | 目检无白边 |
| **B-3/B-5** | Saliency / Alpha matting | 后续 | 仅在 B-1/B-2 未过 G1–G4 时启动 |

**顺序**：A → 提交 → B-1 → B-2 →（过门禁）→ B-4 → 视门禁决定是否把默认改回 `true`。

---

## 1. 现状差距（Codex 已做 vs 还需做）

| 项 | 现状 | 需改 |
|---|---|---|
| photo/cartoon 默认 | `removeBackground: true`（**吞主体根因**） | 改 `false` |
| `defaultSettings` | `removeBackground: true` | 改 `false` |
| `extractSubject` | 无 `reliable` | 新增可靠度指标与判定 |
| `pipeline/index.ts` | 已接入去背景，但无「不可信回退」 | 加 `reliable` 门 |
| 背景判定 | 单像素 `maxNeighborDelta>edge`（软边失效） | 换多尺度 ridge |
| 掩码精修 | 无 | 加 guided filter |
| 边缘白边 | 无处理 | 加 despill |

---

## 2. 阶段 A（立即、安全）

### A-1 `src/app/state.tsx:56`

```diff
   protectFeatures: true,
-  removeBackground: true,
+  removeBackground: false,
   showCodes: true,
```

### A-2 `src/core/post/modePresets.ts`

```diff
 photo: {
   ...
   protectFeatures: true,
-  removeBackground: true
+  removeBackground: false
 },
 cartoon: {
   ...
   protectFeatures: true,
-  removeBackground: true
+  removeBackground: false
 },
```
（`lineart` / `pixel` 已是 `false`，不动。）

### A-3 `src/core/pipeline/subject.ts` — 新增可靠度

**常量**（加在现有 `SUBJECT_*` 常量旁）：

```ts
export const SUBJECT_RATIO_MIN = 0.15;      // 主体占比下限
export const SUBJECT_COMPONENT_MIN = 0.55;  // 最大连通域 / 主体 下限
export const SUBJECT_BBOX_MAX = 0.98;       // 主体包围盒占全幅上限
```

**接口扩展**：

```ts
export interface SubjectResult {
  cells: Uint8ClampedArray;
  mask: Uint8Array;
  bbox: { x0: number; y0: number; x1: number; y1: number } | null;
  reliable: boolean;
  subjectRatio: number;
  largestComponentRatio: number;
}
```

**新增指标函数**（放在 `dropTinyIslands` 之后）：

```ts
interface SubjectMetrics {
  subjectCount: number;
  largestComponent: number;
}

function subjectMetrics(mask: Uint8Array, width: number, height: number): SubjectMetrics {
  const length = width * height;
  const seen = new Uint8Array(length);
  const stack: number[] = [];
  let subjectCount = 0;
  let largestComponent = 0;
  for (let i = 0; i < length; i += 1) if (mask[i] === 1) subjectCount += 1;
  for (let start = 0; start < length; start += 1) {
    if (mask[start] !== 1 || seen[start]) continue;
    let size = 0;
    stack.length = 0;
    stack.push(start);
    seen[start] = 1;
    while (stack.length) {
      const at = stack.pop()!;
      size += 1;
      const x = at % width;
      const y = Math.floor(at / width);
      if (x > 0 && mask[at - 1] === 1 && !seen[at - 1]) { seen[at - 1] = 1; stack.push(at - 1); }
      if (x < width - 1 && mask[at + 1] === 1 && !seen[at + 1]) { seen[at + 1] = 1; stack.push(at + 1); }
      if (y > 0 && mask[at - width] === 1 && !seen[at - width]) { seen[at - width] = 1; stack.push(at - width); }
      if (y < height - 1 && mask[at + width] === 1 && !seen[at + width]) { seen[at + width] = 1; stack.push(at + width); }
    }
    if (size > largestComponent) largestComponent = size;
  }
  return { subjectCount, largestComponent };
}

/** 统一出口：不可信即回退全主体（等价不去背景）。 */
function fallbackAllSubject(data: Uint8ClampedArray, width: number, height: number, subjectRatio: number, largestComponentRatio: number): SubjectResult {
  return {
    cells: new Uint8ClampedArray(data),
    mask: new Uint8Array(width * height).fill(1),
    bbox: { x0: 0, y0: 0, x1: width - 1, y1: height - 1 },
    reliable: false,
    subjectRatio,
    largestComponentRatio
  };
}
```

**`extractSubject` 收尾替换**（现有 `const cleanedMask = ...` 之后整体替换）：

```ts
  const cleanedMask = dropTinyIslands(fillSmallHoles(mask, width, height, holeMax), width, height, minArea);
  const metrics = subjectMetrics(cleanedMask, width, height);
  const subjectRatio = metrics.subjectCount / length;
  const largestComponentRatio = metrics.subjectCount ? metrics.largestComponent / metrics.subjectCount : 0;

  const cells = new Uint8ClampedArray(data);
  let x0 = Number.POSITIVE_INFINITY;
  let y0 = Number.POSITIVE_INFINITY;
  let x1 = Number.NEGATIVE_INFINITY;
  let y1 = Number.NEGATIVE_INFINITY;
  for (let y = 0; y < height; y += 1) {
    for (let x = 0; x < width; x += 1) {
      const index = y * width + x;
      if (cleanedMask[index]) {
        x0 = Math.min(x0, x); y0 = Math.min(y0, y);
        x1 = Math.max(x1, x); y1 = Math.max(y1, y);
      } else {
        cells[index * 4 + 3] = 0;
      }
    }
  }
  if (!Number.isFinite(x0)) return fallbackAllSubject(data, width, height, subjectRatio, largestComponentRatio);

  const bboxRatio = ((x1 - x0 + 1) * (y1 - y0 + 1)) / length;
  const reliable =
    subjectRatio >= SUBJECT_RATIO_MIN &&
    largestComponentRatio >= SUBJECT_COMPONENT_MIN &&
    bboxRatio <= SUBJECT_BBOX_MAX;
  if (!reliable) return fallbackAllSubject(data, width, height, subjectRatio, largestComponentRatio);

  return { cells, mask: cleanedMask, bbox: { x0, y0, x1, y1 }, reliable: true, subjectRatio, largestComponentRatio };
```

**并把现有的「边界无连续背景」早退分支**改为走统一出口：

```ts
    if (!result.reliable) {
      return fallbackAllSubject(data, width, height, 1, 1);   // 原：all=1 全主体
    }
```

### A-4 `src/core/pipeline/index.ts:74-75` — 加可信度门

```diff
   const removeBackground = options.removeBackground === true;
-  const subject: SubjectResult | null = removeBackground ? extractSubject(source) : null;
+  const subjectRaw: SubjectResult | null = removeBackground ? extractSubject(source) : null;
+  // A.2：抠图不可信 → 完全按「未去背景」处理（等价 removeBackground=false）
+  const subject: SubjectResult | null = subjectRaw && subjectRaw.reliable ? subjectRaw : null;
```

> 该门同时治掉 `docs/39` P2-2「纯色图 → 空图纸」（纯色图 subjectRatio/component 必然不达标 → 回退全主体）。

---

## 3. 阶段 B（稳健去背景）

### B-0 常量调整（`subject.ts`）

```diff
-export const SUBJECT_TOL = 13;
-export const SUBJECT_EDGE = 18;
+export const SUBJECT_TOL = 6;    // 收紧：不再靠颜色吞主体
+export const SUBJECT_EDGE = 12;  // 多尺度脊阈值
+export const RIDGE_SCALES = [2, 4, 8] as const;
+export const GUIDED_RADIUS = 4;
+export const GUIDED_EPS = 1e-3;
```

### B-1 多尺度结构梯度（`subject.ts` 新增）

```ts
function integralOf(values: Float64Array, width: number, height: number): Float64Array {
  const W = width + 1;
  const out = new Float64Array(W * (height + 1));
  for (let y = 0; y < height; y += 1) {
    let rowSum = 0;
    for (let x = 0; x < width; x += 1) {
      rowSum += values[y * width + x];
      out[(y + 1) * W + (x + 1)] = out[y * W + (x + 1)] + rowSum;
    }
  }
  return out;
}

function rectSum(integral: Float64Array, width: number, x0: number, y0: number, x1: number, y1: number): number {
  const W = width + 1;
  return integral[y1 * W + x1] - integral[y0 * W + x1] - integral[y1 * W + x0] + integral[y0 * W + x0];
}

/**
 * B-1 多尺度结构梯度脊：ridge(p) = max_s ΔE76( innerMean_s(p), ringMean_s(p) )
 * 软轮廓在粗尺度（s=4/8）仍成脊，补单像素梯度失效（docs/39 §4.3）。
 */
export function buildRidge(data: Uint8ClampedArray, width: number, height: number): Float64Array {
  const length = width * height;
  const L = new Float64Array(length);
  const A = new Float64Array(length);
  const B = new Float64Array(length);
  for (let i = 0; i < length; i += 1) {
    const lab = srgbRgbToLab(rgbAt(data, i * 4));
    L[i] = lab[0]; A[i] = lab[1]; B[i] = lab[2];
  }
  const iL = integralOf(L, width, height);
  const iA = integralOf(A, width, height);
  const iB = integralOf(B, width, height);
  const ridge = new Float64Array(length);
  for (let y = 0; y < height; y += 1) {
    for (let x = 0; x < width; x += 1) {
      let best = 0;
      for (const s of RIDGE_SCALES) {
        const inX0 = Math.max(0, x - s), inX1 = Math.min(width, x + s + 1);
        const inY0 = Math.max(0, y - s), inY1 = Math.min(height, y + s + 1);
        const outX0 = Math.max(0, x - 2 * s), outX1 = Math.min(width, x + 2 * s + 1);
        const outY0 = Math.max(0, y - 2 * s), outY1 = Math.min(height, y + 2 * s + 1);
        const nIn = (inX1 - inX0) * (inY1 - inY0);
        const nOut = (outX1 - outX0) * (outY1 - outY0);
        const nRing = nOut - nIn;
        if (nIn <= 0 || nRing <= 0) continue;
        const inL = rectSum(iL, width, inX0, inY0, inX1, inY1) / nIn;
        const inA = rectSum(iA, width, inX0, inY0, inX1, inY1) / nIn;
        const inB = rectSum(iB, width, inX0, inY0, inX1, inY1) / nIn;
        const outLs = rectSum(iL, width, outX0, outY0, outX1, outY1);
        const outAs = rectSum(iA, width, outX0, outY0, outX1, outY1);
        const outBs = rectSum(iB, width, outX0, outY0, outX1, outY1);
        // ring = 外窗 − 内窗；`inL * nIn` 即内窗 Lab 和
        const ringL = (outLs - inL * nIn) / nRing;
        const ringA = (outAs - inA * nIn) / nRing;
        const ringB = (outBs - inB * nIn) / nRing;
        const d = ciede76([inL, inA, inB], [ringL, ringA, ringB]);
        if (d > best) best = d;
      }
      ridge[y * width + x] = best;
    }
  }
  return ridge;
}
```

> 注：`inL * nIn` 即内窗 L 通道之和；三通道同理（`outAs - inA * nIn`、`outBs - inB * nIn`）。

**`floodColorBackground` 改用 ridge**（替换梯度判据、并在函数开头构建一次）：

```ts
function floodColorBackground(data, width, height, tol, edge): { bg: Uint8Array; reliable: boolean } {
  const ridge = buildRidge(data, width, height);   // 新增
  ...
      if (ciede76(bgLab, lab) > tol) continue;
      if (ridge[next] > edge) continue;            // 原 maxNeighborDelta(...) > edge
  ...
}
```
（原 `maxNeighborDelta` 可删除或保留给别处；不再使用。）

### B-2 Guided filter 掩码精修（`subject.ts` 新增）

```ts
function boxMean(values: Float64Array, width: number, height: number, radius: number): Float64Array {
  const integral = integralOf(values, width, height);
  const out = new Float64Array(width * height);
  for (let y = 0; y < height; y += 1) {
    for (let x = 0; x < width; x += 1) {
      const x0 = Math.max(0, x - radius), x1 = Math.min(width, x + radius + 1);
      const y0 = Math.max(0, y - radius), y1 = Math.min(height, y + radius + 1);
      out[y * width + x] = rectSum(integral, width, x0, y0, x1, y1) / ((x1 - x0) * (y1 - y0));
    }
  }
  return out;
}

/**
 * B-2 引导滤波精修掩码（He et al. 2010，O(N) 确定性，零依赖）。
 * guide = 原图亮度；p = 二值掩码；输出 q>0.5 为掩码。
 */
export function refineMaskGuided(
  data: Uint8ClampedArray,
  mask: Uint8Array,
  width: number,
  height: number,
  radius = GUIDED_RADIUS,
  eps = GUIDED_EPS
): Uint8Array {
  const length = width * height;
  const guide = new Float64Array(length);
  const p = new Float64Array(length);
  const gg = new Float64Array(length);
  const gp = new Float64Array(length);
  for (let i = 0; i < length; i += 1) {
    const offset = i * 4;
    guide[i] = (0.2126 * data[offset] + 0.7152 * data[offset + 1] + 0.0722 * data[offset + 2]) / 255;
    p[i] = mask[i] ? 1 : 0;
    gg[i] = guide[i] * guide[i];
    gp[i] = guide[i] * p[i];
  }
  const meanI = boxMean(guide, width, height, radius);
  const meanP = boxMean(p, width, height, radius);
  const meanGG = boxMean(gg, width, height, radius);
  const meanGP = boxMean(gp, width, height, radius);
  const a = new Float64Array(length);
  const b = new Float64Array(length);
  for (let i = 0; i < length; i += 1) {
    const varI = meanGG[i] - meanI[i] * meanI[i];
    const cov = meanGP[i] - meanI[i] * meanP[i];
    a[i] = cov / (varI + eps);
    b[i] = meanP[i] - a[i] * meanI[i];
  }
  const meanA = boxMean(a, width, height, radius);
  const meanB = boxMean(b, width, height, radius);
  const out = new Uint8Array(length);
  for (let i = 0; i < length; i += 1) {
    out[i] = meanA[i] * guide[i] + meanB[i] >= 0.5 ? 1 : 0;
  }
  return out;
}
```

**接入 `extractSubject`**：把 `cleanedMask` 计算改为——

```ts
  const roughMask = fillSmallHoles(mask, width, height, holeMax);
  const refined = refineMaskGuided(data, roughMask, width, height);
  const cleanedMask = dropTinyIslands(refined, width, height, minArea);
```

### B-4 边缘去色污染（去掉主体边缘残留白边）

```ts
/**
 * B-4：主体边缘 despill —— 贴背景的主体像素若更接近背景色，用主体邻域中值色按强度混合。
 */
export function despillEdge(
  cells: Uint8ClampedArray,
  mask: Uint8Array,
  width: number,
  height: number,
  strength = 0.6
): void {
  const sub = new Uint8ClampedArray(cells);
  for (let y = 0; y < height; y += 1) {
    for (let x = 0; x < width; x += 1) {
      const index = y * width + x;
      if (!mask[index]) continue;
      let touchesBg = false;
      const samples: number[] = [];
      for (let dy = -1; dy <= 1; dy += 1) {
        for (let dx = -1; dx <= 1; dx += 1) {
          if (!dx && !dy) continue;
          const nx = x + dx, ny = y + dy;
          if (nx < 0 || ny < 0 || nx >= width || ny >= height) continue;
          const n = ny * width + nx;
          if (mask[n]) samples.push(n * 4);
          else touchesBg = true;
        }
      }
      if (!touchesBg || samples.length < 2) continue;
      const off = index * 4;
      for (let c = 0; c < 3; c += 1) {
        const vals = samples.map((o) => sub[o + c]).sort((p, q) => p - q);
        const median = vals[Math.floor(vals.length / 2)];
        cells[off + c] = Math.round(sub[off + c] * (1 - strength) + median * strength);
      }
    }
  }
}
```
**接入**：`extractSubject` 生成 `cells` 后调用 `despillEdge(cells, cleanedMask, width, height)`。

### B-3 / B-5（条件启用，不在本次）

- **B-3 Saliency（spectral residual）**：仅在 B-1/B-2 未过 G1–G4 时启动。做法：灰度降采样到 64×64 → FFT → `log|A|` 减去其均值滤波 → `exp(residual + i·phase)` → IFFT → 平方 → 高斯模糊 → 上采样。需要自实现 radix-2 FFT（约 50 行，确定性）。**自实现按论文，不抄无证仓库**。
- **B-5 Alpha matting**：软边最强但需解稀疏线性系统；建议降采样求解 + 上采样。仅在 B-3 仍不达标时评估。

---

## 4. 测试（`tests/unit/subject.test.ts` 增补）

### 4.1 合成「白底白身软边」夹具（可复现，进 CI）

```ts
function softWhiteSubject(size = 240): CellImage {
  const data = new Uint8ClampedArray(size * size * 4);
  const bg: [number, number, number] = [250, 250, 250];
  const body: [number, number, number] = [255, 255, 255];
  const cx = size / 2, cy = size / 2, r = size * 0.34;
  for (let y = 0; y < size; y += 1) {
    for (let x = 0; x < size; x += 1) {
      const d = Math.hypot(x - cx, y - cy) / r;
      // 软边：d∈[0.92,1.08] 之间从 body 过渡到 bg
      const t = Math.min(1, Math.max(0, (d - 0.92) / 0.16));
      const c: [number, number, number] = [
        Math.round(body[0] * (1 - t) + bg[0] * t),
        Math.round(body[1] * (1 - t) + bg[1] * t),
        Math.round(body[2] * (1 - t) + bg[2] * t)
      ];
      data.set(c, (y * size + x) * 4);
      data[(y * size + x) * 4 + 3] = 255;
    }
  }
  // 关键彩色特征（眼），确保主体有可辨识的语义点
  for (const [ex, ey] of [[cx - 14, cy - 12], [cx + 14, cy - 12]]) {
    for (let dy = -3; dy <= 3; dy += 1) for (let dx = -3; dx <= 3; dx += 1) {
      const px = Math.round(ex + dx), py = Math.round(ey + dy);
      data.set([35, 40, 48], (py * size + px) * 4);
    }
  }
  return { width: size, height: size, data };
}
```

### 4.2 门禁断言

```ts
import { extractSubject, SUBJECT_RATIO_MIN } from '../../src/core/pipeline/subject';

describe('extractSubject 门禁（白底白身软边）', () => {
  const img = softWhiteSubject();

  it('不可信时回退全主体（绝不吞主体/空图纸）', () => {
    const r = extractSubject(img);
    if (!r.reliable) {
      expect([...r.mask].every((v) => v === 1)).toBe(true);
      expect(r.bbox).toEqual({ x0: 0, y0: 0, x1: img.width - 1, y1: img.height - 1 });
    }
  });

  it('可信时主体连通且覆盖头部区域', () => {
    const r = extractSubject(img);
    if (r.reliable) {
      expect(r.subjectRatio).toBeGreaterThanOrEqual(SUBJECT_RATIO_MIN);
      expect(r.largestComponentRatio).toBeGreaterThan(0.9);
      // 头部/躯干中心必在主体内
      const center = Math.floor(img.height / 2) * img.width + Math.floor(img.width / 2);
      expect(r.mask[center]).toBe(1);
    }
  });

  it('纯色图不产生空图纸', () => {
    const flat = { width: 60, height: 60, data: (() => {
      const d = new Uint8ClampedArray(60 * 60 * 4);
      for (let i = 0; i < 60 * 60; i += 1) { d.set([220, 30, 30], i * 4); d[i * 4 + 3] = 255; }
      return d;
    })() };
    const r = extractSubject(flat);
    const subject = [...r.mask].filter((v) => v === 1).length;
    expect(subject).toBeGreaterThan(0);
  });
});
```

### 4.3 真实样例门禁（`.tools/acceptance/gate-b.mjs`，沿用 docs/39 的 Playwright 环境）

对 `rabbit-source.png` 断言：`reliable=true`、头部矩形区 mask 覆盖 `>0.8`、`largestComponentRatio>0.9`、开启态主体格数 ≥ 关闭态×0.9、`sample-photo.jpg` 无「透明锯齿边框 + 背景残块」。

---

## 5. 执行顺序与验证命令

```sh
# 1) 落 A（默认关闭 + reliable 兜底）
npx tsc --noEmit
npx vitest run tests/unit/subject.test.ts tests/unit/modePresets.test.ts tests/unit/ui-settings.test.ts
#    → 关闭态必须逐格等于上一版（复用 docs/39 的 compare-off.mjs）

# 2) 提交 A
git add -A && git commit -m "fix(v1.2): 去背景默认关闭 + reliable 可信度兜底（止血）"

# 3) 落 B-1 / B-2 / B-4，跑门禁
npm run dev            # 另一终端
node .tools/acceptance/run.mjs
node .tools/acceptance/gate-b.mjs        # 输出 G1–G7 逐项
npx vitest run

# 4) 门禁 G1–G4 全过 → 才把 photo/cartoon 的 removeBackground 改回 true；否则保持 false
```

---

## 6. 交给 Codex 的提示词（可直接粘贴）

> 你负责实现 `docs/43`。
> **阶段 A（必做，先提交）**：① `src/app/state.tsx` 的 `defaultSettings.removeBackground` 改 `false`；② `src/core/post/modePresets.ts` 的 `photo`/`cartoon` 预设 `removeBackground` 改 `false`；③ 按 `docs/43 §A-3` 给 `src/core/pipeline/subject.ts` 增加 `reliable`/`subjectRatio`/`largestComponentRatio` 与统一回退出口 `fallbackAllSubject`；④ 按 `§A-4` 在 `src/core/pipeline/index.ts` 加「不可信 → 视为未去背景」的门。
> **阶段 B（A 通过后）**：按 `§3` 实现 B-1 多尺度 ridge（替换现有 `maxNeighborDelta` 判据，`TOL=6/EDGE=12`）、B-2 guided filter 精修、B-4 边缘 despill。
> **测试**：按 `§4` 增补单测；真实样例门禁用 `§4.3`。
> **纪律**：不改匹配/限色/合并/描边算法；不改对外签名（`SubjectResult` 只增字段）；禁止新增依赖；`npx tsc --noEmit` 必过；**默认开启态必须满足「主体格数 ≥ 关闭态×0.9 且无空图纸」，否则一律回默认关闭**。
> 产出：代码 + `docs/44-v1.2-去背景A+B修订自测报告.md`（含 G1–G7 实测与 before/after）。若 `§3` 的代码片段与现有实现冲突，以**本文件语义**为准并说明偏差。

---

## 7. 诚实边界

- 本文代码为**规格级参考实现**，阈值（`TOL=6`、`EDGE=12`、`s∈{2,4,8}`、`ratio/component` 阈值、`GUIDED_*`）需 Codex/Claude 在可运行环境用 `rabbit-source.png` **实测校准**。
- B-1/B-2 是否能让本类「白底白身软阴影」过门禁**尚未验证**（docs/39 已证单一颜色判据必然失败）；未过则按 §3 B-3/B-5 升级，或如实判定「需 alpha matting / 学习式抠图」并另立任务。
- 我（DeepSeek）未在本环境跑真实 jpg（`.tools/bench/` 无 JPEG 解码器）。
