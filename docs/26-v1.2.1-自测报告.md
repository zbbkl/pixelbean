# 26 · v1.2.1 自测报告

- 日期：2026-09-07
- 执行 Agent：Codex
- 验收契约：docs/25 §5 A/B 组矩阵
- 基线与冻结：`main @ bb02cc8`（143 测试全绿），本版不改 v1.2 对外签名与默认后处理行为

## 命令与测试

| 项 | 结果 |
|---|---|
| `npm test` | ✅ 155 例全绿（既有 143 + 新增 12） |
| `npm run build` | ✅ `tsc --noEmit` + Vite 构建成功 |
| v1.2.1 Playwright 六档视口 | ✅ `.tools/v12dot1-shots/results.json`：`failed=0` |
| 浏览器 console/pageerror | ✅ 六档视口均为 0 |

## A 组 UI/配置验收

| 验收点 | 结果 | 证据 |
|---|---|---|
| 冷启动目标模式=照片、protectFeatures 开、参数=照片预设 | ✅ | Playwright 读取 select/checkbox |
| 无「取消」按钮；过期转换由 seq 丢弃 | ✅ | 六档视口断言 count=0 |
| 手动改 K→自定义、回选照片恢复预设 | ✅ | 桌面 1440/1024 交互通过 |
| 底板 32/52/78/104/120，默认 52 高亮 | ✅ | Playwright chips 与 active 断言 |
| 限色 chips 24…313、自选 K 上限 400 | ✅ | Playwright 文本 + `max=400` |
| 手机底栏不遮挡 | ✅ | app-body 底边==nav 顶边；控制条/统计面板无越界 |
| 全屏开关与 320 无横向溢出 | ✅ | 320/360/390 截图通过 |

代码落点：`boardPresets.ts`、`state.tsx`、`modePresets.ts`、`options.ts`（`MAX_COLOR_STEPS`）、`ParamPanel.tsx`、`App.tsx`（删取消）、`mobile-viewer.css`（`flex:0 0 auto` 收口）。

## B 组算法验收

- `features.ts` 新增平衡双类“边界格”仲裁：`BOUNDARY_DE_Y=0.15 / BOUNDARY_SHARE_MIN=0.30 / BOUNDARY_MARGIN=3.0`，仅在 features 开启、双类块状高对比时改写；不写 protect。
- 新增 `regionClean`（`src/core/post/regionClean.ts`）：4-邻中 3 个同色且 ΔE≤8 时才并；跳过 protect/空格；默认不进模式预设，经 `convertPipeline(..., true)` 可编程接入。
- 测试：balanced boundary 合成两态、regionClean 5 例、pipeline opt-in 1 例；新增共 12 例，确定性双跑一致。

内置样例度量（`.tools/v12dot1-report/metrics.json`）：

| 路径 | 耗时 | 色数 | 占用 |
|---|---:|---:|---:|
| 卡通 features off | 67.8ms | 9 | 2494 |
| 卡通 features on | 98.5ms | 9 | 2494 |
| 卡通 features on + regionClean | 86.8ms | 9 | 2494 |
| 照片 features off | 38.4ms | 72 | 2668 |
| 照片 features on（v1.2.1 默认） | 47.2ms | 72 | 2668 |

截图存档：`.tools/v12dot1-shots/photo-features-on.png`、`photo-features-off.png`、`cartoon-features-on.png`、`cartoon-features-off.png`。

## 语义修订登记

- docs/01 A 档位 chips 与 docs/03 §9.5 默认照片说明已同步。
- 删除 UI 取消后，转换中止依赖 worker 端 seq 丢弃；`cancelConvert` reducer 分支保留为历史兼容死代码并注释。
- `regionClean` 未并入默认预设；outline 连续性微调按 docs/25 §6.2 未并入，交 Claude 按对照效果决议。

## 截图存档

- `.tools/v12dot1-shots/tiny-320-preview.png` / `tiny-320-fullscreen.png` / `tiny-320-stats.png`
- `.tools/v12dot1-shots/android-360-preview.png` / `android-360-fullscreen.png` / `android-360-stats.png`
- `.tools/v12dot1-shots/phone-390-preview.png` / `phone-390-fullscreen.png` / `phone-390-stats.png`
- `.tools/v12dot1-shots/desktop-1440-default.png` / `desktop-1024-default.png` / `ipad-768.png`

## 残余风险

- 特征/边界扫描使开启细节保护的照片路径较关闭多约 9ms（58 格内置照片样例），仍在 docs/19 Q4 的“同数量级”预算内；默认路径即开启该能力。
- 边界仲裁与 regionClean 的效果仍需 Claude Code 与人工在真实角色图上复核；本版保守保留可开关边界。
