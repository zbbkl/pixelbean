# 02 · 架构设计

> 用途：定义模块边界与接口形状，让 Codex 能**分模块实现并逐步自测**，让 Claude Code 有明确的审查对象。
> 硬性工程纪律（吸取 Zippland page.tsx 135KB / bead-grid app.js 264KB 单体教训）：**任何源文件 ≤ 400 行**；核心算法零 DOM；全程确定性。

## 1. 技术选型与理由

| 项 | 选择 | 理由 |
|---|---|---|
| 构建 | **Vite 5/6 + React 18 + TypeScript** | 纯前端静态产物；主流栈降低多 Agent 实现/审查的出错率；画布重绘由我们控制，React 只做状态与面板 |
| UI | 无组件库，纯 CSS + 原生控件 | 少一个依赖就少一份 Agent 幻觉空间；自定义控件样式符合工具类产品气质 |
| 转换 | **Web Worker**（`new Worker(...,{type:'module'})`） | 主线程不卡；大图可取消 |
| 颜色/算法 | 自研 `src/core/`（零依赖） | 算法是本产品核心价值，且调研证明零依赖完全可行 |
| 测试 | **Vitest**（core 单测 + golden fixture） | 确定性回归；Playwright e2e 可选（P2） |
| 导出 | Canvas.toBlob PNG + 打印样式表（浏览器"打印→PDF"） | v1 不引 jspdf；P2 再评估 |
| 部署 | 纯静态 `dist/` → GitHub Pages / Vercel / Nginx | 无后端零运维 |

依赖清单（目标 **运行时零依赖或 ≤1**；devDeps: vite/typescript/vitest/@vitejs/plugin-react 等即可）。

## 2. 目录结构（目标形态）

```
pixelbean/
├─ index.html / package.json / vite.config.ts / tsconfig.json
├─ public/samples/                 # 内置示例图（照片/像素角色 各1，体积小）
├─ data/                           # 权威色表数据（docs/04 契约；不直接进 bundle）
│  ├─ palette.schema.json
│  ├─ palettes/mard-291.json …
│  ├─ provenance/  scripts/
├─ src/
│  ├─ main.tsx  App.tsx  app.css
│  ├─ app/                         # React 壳（允许薄 UI 逻辑，禁止算法）
│  │  ├─ components/               # UploadZone / ParamPanel / PreviewCanvas /
│  │  │                            #   StatsPanel / ExportBar / HoverBar / SamplePicker
│  │  ├─ boardPresets.ts           # 常用尺寸预设常量表（可配置）
│  │  └─ state.tsx                 # useReducer + context；options 单一来源
│  ├─ core/                        # ★ DOM-free，全部可单测（本目录是产品核心）
│  │  ├─ color/srgb.ts  lab.ts  ciede2000.ts  adjust.ts     # docs/03 §1/§3
│  │  ├─ palette/types.ts  loader.ts  validate.ts           # docs/04 契约
│  │  ├─ pipeline/background.ts  downsample.ts  match.ts
│  │  │            colorLimit.ts  dither.ts  index.ts        # docs/03 §2/§4/§5
│  │  ├─ pattern.ts                # Pattern 类型 + RLE 序列化 + 统计（docs/03 §6.1）
│  │  └─ render/drawGrid.ts        # 画布绘制 + 标注 LOD（docs/03 §6.2/§6.3；只吃 ctx2d）
│  ├─ worker/convert.worker.ts     # 极薄：收消息 → 调 core/pipeline → 回传
│  ├─ export/png.ts  csv.ts  print.ts
│  ├─ generated/palettes.ts        # build:palettes 生成（内置集注册表）
│  └─ types.ts                     # 跨层公共类型
└─ tests/
   ├─ unit/…（与 src/core 对齐）
   └─ fixtures/                    # golden：入参样本 + 期望 hash / 期望 Pattern JSON
```

## 3. 核心类型与模块接口（Codex 照此实现，勿改签名；要改先改 docs）

```ts
// src/types.ts
type PaletteId = string
type BgMode = 'white' | 'black'
type DownsampleMode = 'average' | 'dominant'          // 默认 average
type DitherMode = 'none' | 'floyd-steinberg'          // 默认 none
interface ConvertOptions {
  paletteId: PaletteId
  width: number; height: number        // 目标网格（已由 §geometry 算好，锁宽高比）
  bg: BgMode
  mode: DownsampleMode
  maxColors: number | null             // null=不限（默认）
  dither: DitherMode                   // maxColors 非空时忽略（UI 置灰）
  adjust?: { brightness: number; contrast: number; saturation: number } // 默认全 0（P1）
}
// 网格尺寸语义在 UI 层完成：preset(方形, contain 留空) 或 custom(长边=N) → {width,height}
interface Pattern {
  paletteId: PaletteId
  width: number; height: number
  cells: Int16Array                    // 每格 = palette 色索引；-1 = 空(留空行/背景空)
  codes: string[]                      // index→code（对齐 labPalette 顺序）
  options: ConvertOptions
}
interface ColorStat { index: number; code: string; name: string | null; hex: string | null; count: number; ratio: number }
```

```ts
// src/core/palette/loader.ts
loadPalette(json: unknown): LoadedPalette        // 校验(docs/04 §1) → 补 rgb → solids=kind==='solid'
                                                  // → lab: Float64Array(solids.length*3) + indexByCode
// src/core/pipeline/
deriveGridSize(srcW, srcH, mode: {preset: {w,h,contain:true}} | {longEdge:number}): {width,height}
downsample(img: {width,height,data:Uint8ClampedArray}, W, H, mode, bg): Uint8ClampedArray // W*H*4 sRGB
matchGrid(cellsRgba, lp: LoadedPalette): Pattern    // 内部 ΔE76 初筛 + ΔE2000 定夺（docs/03 §3.3）
limitColors(p: Pattern, lp, maxK): Pattern          // §4 关键色保护
dither(cellsRgba, lp): Pattern                       // §5 FLS
convertPipeline(srcImg, options, lp): Pattern        // background→downsample→match→limit?→dither?
                                                      // 一个入口给 worker 与单测复用
countByColor(p: Pattern): ColorStat[]                // 断言 Σcount === w*h - 空数
// src/core/render/drawGrid.ts
drawGrid(ctx, p, lp, view: {zoom,pan,showCodes,showGridLines,hover?}): void
// src/export/
exportPng(p, lp, mode: 'codes'|'colors'|'coordinates', scalePxPerCell): Promise<Blob>
exportCsv(stats: ColorStat[], doneSet: Set<number>): Blob   // UTF-8 BOM
printSheet(p, lp): void                            // 注入打印样式/隐藏节点（或跳打印视图）
```

## 4. 数据流与状态

```
上传/示例图 ─→ createImageBitmap → {bitmap,width,height}
options(面板) ─(防抖 300ms)→ 派发 convert(id)
  └→ worker: convertPipeline(ImageData/ImageBitmap, options, paletteData)
       → Pattern(cells:Int16Array transferable) + stats
  ← 主线程: 校验 id 最新 → 存 state.pattern → StatsPanel 渲染表
                              → PreviewCanvas 重绘（drawGrid）
导出: 复用同一 drawGrid 到离屏大 canvas（不依赖屏幕缩放）→ toBlob / 打印
```

- **state 单一来源**：`state = { image, options, pattern, stats, lastSeq }`；`useReducer`。旧转换结果按 `seq` 丢弃（worker 无法取消 JS 循环时，靠 seq 校验保证 UI 只采纳最新）。
- 图片进 worker：`OffscreenCanvas` 可用则 transfer，否则 `ImageData`（`data.buffer` transfer）。v1 允许降级：worker 内直接用主线程传入的 ImageBitmap 的像素。
- 上限保护：`maxCells = 150_000`（≈ 长边 390 方形）；超限 UI 确认或拒绝；最大原图边 ≤ 12000px（先压缩取样再进 worker）。

## 5. 渲染（PreviewCanvas）要点

- 单 `<canvas>`；内部逻辑坐标 = 格坐标；`zoom = cellPx（每格像素）`，范围 1..96（`cellPx<1` 时缩小整体画面）。
- 滚轮缩放以光标为锚点；空格/中键拖拽平移；触控板双指平移（移动端基础支持）。
- 绘制 LOD（docs/03 §6.3）：cellPx<14 纯色块；≥14 加格线+色号文字；文字黑/白按相对亮度（§6.2）。悬停命中 = 逆变换取格 → HoverBar。
- 设备像素比处理：canvas 物理尺寸 = css×dpr；`ctx.setTransform` 统一管理，不用 `scale` 拼接避免模糊。
- 格子数大（> 40k）时 `drawGrid` 按行分块 `requestAnimationFrame`，避免单帧卡顿；v1 可接受一次性画完（≤150k 格 Canvas 2D fillRect 数量级没问题，实测不足再分块）。

## 6. Worker 协议（convert.worker.ts）

```ts
// → worker: { seq, kind:'convert', img:{width,height,data?|bitmap?}, options, palette: LoadedPalette(JSON+lab 由 loader 在主线程算好传入，worker 只消费) }
// ← main:  { seq, kind:'result', pattern:{width,height,cells(Int16Array transfer),codes}, stats }
// ← main:  { seq, kind:'error', message }
// 取消：主线程递增 seq 并丢弃过期结果；不在 worker 里做抢占式取消（P1 可加 SharedArrayBuffer 标志）
```

## 7. 工程文件说明（给 Codex 的顺序敏感信息）

- `src/core/**` 不允许 `import` 任何 `src/app/**` 或 DOM API；`render/drawGrid.ts` 只接受 `CanvasRenderingContext2D` 参数（可单测：用 `node-canvas` 或 mock 上下文做形状断言，P1）。
- golden fixture：`tests/fixtures/*.png`（小尺寸 64×64 合成图，无版权）+ `expected.json`（Pattern cells+统计的 sha-256）。改算法必须同步更新 fixture 并写明原因。
- `npm run dev` / `npm run build` / `npm test` 三条命令为验收基线（docs/01 §5）。

## 8. 路线图钩子（本版不实现，但架构上不要堵死）

- **编辑能力**：Pattern 已把 cells 与 palette 解耦 → v1.1 加 `PatchOp[]`（set/erase/floodFill）与撤销栈，落 localStorage + 工程 JSON（RLE 压缩 cells，不内嵌原图）。
- **多板拼接**：`Pattern` 可带 `board: {板型, 摆放(x,y)}[]` 元数据 → 渲染层加辅助线即可。
- **特殊色支持**：loader 保留 kind=special/transparent，UI 加"手动上色模式"即可用。
- 云端 AI 增强（火山即梦型）与后端分享：属独立方向，v1 架构不引入任何服务端假设。

---

# V1.1 架构增量（拼豆工艺后处理）

## 9. 新增目录与模块

```
src/core/post/                 # ★ 工艺后处理（零 DOM、确定性、可单测）
   speckle.ts         §9.1 speckleClean(cells, paletteLab, noiseMax, ΔEthr) → Pattern'
   shadowSimplify.ts  §9.2 shadowSimplify(pattern, level: 0|1|2) → Pattern'
   clusterLimit.ts    §9.3 clusterLimit(pattern, paletteLab, K, protect=true) → Pattern'  // 取代 legacy colorLimit
   outline.ts         §9.4 outline(pattern, paletteLab, τ) → Pattern'                    // 1px，色板内深色
   modePresets.ts     §9.5 modePresets: Record<ModeId, Partial<UiSettings>> + applyMode()
   index.ts           runPostPipeline(pattern, post: PostOptions) → Pattern'
src/app/              追加参数控件：mode 下拉 / K chips / 暗部三档 / 噪点阈值 / 描边开关+τ
src/app/boardPresets.ts  追加 { id:'board-116', label:'116 × 116', side:116 }
```

## 10. 类型增量（src/types.ts）

```ts
interface PostOptions {
  speckleClean: boolean; speckleMax: number;        // 1..3 默认2
  speckleDeltaE: number;                            // 20..40 默认30
  shadowSimplify: 0 | 1 | 2;                        // 0关 1标准 2强
  maxColors: number | null;                         // 既有字段，语义升级为 K（§9.3）
  outline: boolean; outlineTau: number;             // 0.10..0.30 默认0.18
}
type TargetMode = 'photo' | 'cartoon' | 'lineart' | 'pixel' | 'custom';
interface ConvertOptions { /* 既有字段不变 */ post?: PostOptions }
```
`ConvertOptions` 与 Pattern 向后兼容：`maxColors:null + post 全关` = v1 原行为（保证旧 golden 不回归）。

## 11. 实施顺序（v1.1，Codex 用）
1. `src/core/post/modePresets.ts`（组合定义，纯数据）→ 2. `speckle.ts` → 3. `shadowSimplify.ts` → 4. `clusterLimit.ts`（含 legacy colorLimit 保留）→ 5. `outline.ts` → 6. `index.ts runPostPipeline` + 接入 `convertPipeline`（worker 不用改协议，options 透传）→ 7. UI 控件与"自定义"联动 → 8. boardPresets+116 → 9. 色板数据 Perler/Hama（data/scripts 扩展，见 docs/04 §7）→ 10. 测试与 golden、README/docs 同步。
每步 `npm test` 全绿再进下一步；不得破坏既有 56 用例（v1.1 语义变化处以新用例覆盖并在报告记录）。

---

# V1.2 架构增量（P0 视图修复 + 算法优化）

> v1.2 分两个独立立项，各自的任务书与验收见：**`docs/18`（P0 问题修复技术方案，渲染/布局）** 与 **`docs/19`（算法优化设计文档，P1 工艺算法）**。本文档只记录两者带来的架构增量；冲突时以 docs/18、docs/19 为准，先改文档再改代码。

## 12. 渲染模型增量（docs/18 配套）

- **视口化画布**：屏幕预览 canvas 的位图恒定 = 查看区 CSS×dpr（`canvas.style.width/height=100%`），不再随 `zoom/pan/网格尺寸` 增长；缩放/平移 = 画布内变换 + 可见区裁剪。消灭“canvas 位图随 200 格×高 zoom 超浏览器上限→空白”与“滚动条→contentSize 变化→自动 fit 复位”两个根因。
- 新增纯函数模块 `src/app/viewport.ts`（`fitZoom/centerPan/zoomAround/zoomCentered/visibleCellRange`；常量 `ZOOM_MIN=0.05 / ZOOM_MAX=96 / FIT_PAD=16`）；`gesture.ts` 的 `clampPan/PADDING` 保留为唯一边界收口。
- `drawGrid(ctx,p,lp,view,range?)` 追加可选 `range`（默认全图）：导出/打印/既有 render 单测**不传参即行为不变**；`exportPng`/`printSheet` 的全分辨率离屏渲染不受影响。
- fit 触发收窄为白名单（换图/显式适应/停留在 fit 档的布局跳变），**尺寸变化不再无条件重置用户视图**（docs/18 §3.5）。
- 移动端：安全区与高度统一为 CSS 变量（`--app-header-h/--mobile-nav-h/--viewer-top-safe/--viewer-bottom-safe`），建立 z 序表（画布 1 / 悬浮提示 3 / 头 20 / 底栏 30 / 全屏 60 / 弹窗 70+），底部工具条改 3 列网格防窄屏溢出（docs/18 §5）。

## 13. 算法层增量（docs/19 配套）

- 降采样阶段新增**特征识别**：`src/core/pipeline/features.ts` 的 `downsampleWithFeatures(...)` 返回 `{cells, protect}`；`protect: Uint8Array`（内容网格 1 字节/格）是唯一新增的内部数据，**不进入 Pattern / worker 协议 / 草稿 / RLE**（只在 worker 内 convertPipeline 调用链传递；contain 扩板用 `placeProtectMask` 同步搬移）。
- 类型增量：`ConvertOptions.features?: {enabled:boolean}`（缺省=false，向后兼容）；`UiSettings.protectFeatures: boolean`。worker 协议不变（features 随 options 透传）。
- 后处理 V2：`speckleCleanV2 / shadowSimplifyV2 / clusterLimitV2`（speckle.ts / shadowSimplify.ts / clusterLimit.ts 各自新增导出，v1 同名函数原样保留）；`runPostPipeline(pattern,palette,post,protect=null)` 追加可选参并内部改调 V2。算法细节、阈值、语义修订（S1–S3）以 docs/19 为唯一契约。
- 架构边界不变：核心算法仍零 DOM、确定性、可单测；模块 < 400 行；数据结构（cells/Pattern/RLE/统计）不变。

## 14. v1.2 工程纪律

- docs/18 §8 列出 P0 改动文件白名单（**禁止触碰** `src/core/post|pipeline|color|palette/**`、`src/export/**`、`src/worker/**`、`data/**`）；docs/19 §0.2 列出算法改动的兼容纪律与允许的语义修订方向。
- 既有 112 测试保持全绿；v1.2 新增单测与 fixture 见 docs/18 §6/§7、docs/19 §8；golden 变更只允许在 docs/19 §0.3 允许方向并逐条记录。
