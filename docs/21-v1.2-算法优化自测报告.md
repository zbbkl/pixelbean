# 21 · v1.2 算法优化自测报告

- 日期：2026-09-07
- 执行 Agent：Codex
- 验收契约：docs/19 §8（T21–T30）、docs/03 V1.2 增量
- 范围：P1 后处理与降采样增强；P2 按 docs/01 暂不落地

## 命令与测试

| 项 | 结果 |
|---|---|
| `npm test` | ✅ 142 例全绿（新增 speckle-v2 5 / shadow-v2 4 / cluster-v2 3 / features 3 / viewport 12 / render range 1 / gesture 1 / mode 1 等） |
| `npm run build` | ✅ 构建成功 |
| 确定性 | ✅ V2 路径无随机源，全部排序并列按索引小者优先 |
| 数据结构契约 | ✅ `Pattern.cells`/worker 协议/导出/统计未改；保护掩码只在 worker 内转换链传递 |

## 模块与测试对应

| 模块 | 新增内容 | 覆盖用例 |
|---|---|---|
| `src/core/pipeline/features.ts` | `downsampleWithFeatures` + `placeProtectMask`，细线/点状特征识别与保护掩码 | T23/T24/T26 |
| `src/core/post/speckle.ts` | `speckleCleanV2`：单格 ≥2 边 + 2..N 连通贴附 | T21/T22 |
| `src/core/post/shadowSimplify.ts` | `shadowSimplifyV2`：近中性暗部标准 ≤2 / 强档 1 层，彩色暗部同族小半径 | T28 暗部指标 |
| `src/core/post/clusterLimitV2.ts` | 近似色预合并、边缘色预归并、主色必保、族内主色代表 | T27 |
| `src/core/post/index.ts` | `runPostPipeline(..., protect=null)` 内部切 V2 | T25/T30 |
| `src/core/pipeline/index.ts` | features 开关与掩码流、contain 同步搬移 | T26 |
| UI/类型/预设 | `protectFeatures`、`features`、模式回填 | T29 |

## T21–T30 结果

| # | 结果 | 证据 |
|---|---|---|
| T21 | ✅ | 单格清、四向不同色包围的单格保留 |
| T22 | ✅ | 2 格贴附对并入；protect 格保留 |
| T23 | ✅ | 灰底+细竖/横线+2×2 点簇：写回特征色，protect ≤ cap |
| T24 | ✅ | 关闭开关时直接 `downsample`，不产生 protect 改写（对照由函数开关路径覆盖） |
| T25 | ✅ | protect 掩码贯穿 cluster，特征格不被并入肤色；色数 ≤K |
| T26 | ✅ | `placeProtectMask` 与 `placeContain` 同偏移，留空行保持 0 |
| T27 | ✅ | 黄色主色恒在输出、族内 ≤3 层、ΔE≤2 双胞胎只留大用量者 |
| T28 | ✅ | 黄色渐变 golden：≤24 色、黄系 ≤3 层、L*<45 暗层 ≤1 |
| T29 | ✅ | 卡通预设 `protectFeatures=true`；照片/线稿/像素为 false；手动改参数变 custom |
| T30 | ✅ | 全后处理+outline：空行/背景不产生轮廓杂色，统计色数符合 K/K+1 语义 |

## Golden 差异记录

黄色渐变 fixture `cellsSha256` 由
`2f9046a1…944b` 更新为
`f005e3d3…16d6`（tests/fixtures/golden-yellow-gradient-64x64.json）。

变更方向属于 docs/19 §0.3 允许范围：V2 预合并/主色代表后颜色结构更紧凑，验收指标仍为 ≤24 色、黄色族 ≤3 层、暗层 ≤1；确定性 hash 双跑一致。

## 效果数据

同一内置测试图 `sample-cartoon.png`（609×814，43×58 网格，MARD，卡通模式 K24）：

| 指标 | v1 直接量化匹配 | v1.2 默认 V2 | 变化 |
|---|---:|---:|---:|
| 使用色号数 | 27 | 6 | **-77.8%** |
| ≤2 格暗部杂色块 | 116 | 1 | **-99.1%** |
| 占用格数 | 2494 | 2494 | 0（数据契约不变） |

数据存档：`.tools/v12-report/metrics.json`。开启“保留高对比细节”后内置卡通样例输出 8 色，包含 H05/M04 等特征保护格，符合 docs/19 §4 的形状保护语义。

## 性能记录

同一浏览器环境、同一张测试图：v1.1 手动全链路约 68.6ms，v1.2 默认 V2 约 56.1ms，默认路径无劣化。开启细节保护时约 114.3ms，多出的成本来自 docs/19 Q4 说明中的源像素二次特征扫描；默认档不开启，移动端主流程不触发该额外扫描。

## 残余说明

- 特征识别属于确定性启发式，极端噪点图仍需人工抽查；默认 UI 关闭“保留高对比细节”，只有进入卡通预设或手动打开才启用。
- `.tools/` 不入库；metrics 脚本与结果保留在本机工作区。
