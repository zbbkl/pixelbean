# 12 · v1.1 审查任务书（Claude Code 终审）

> v1.1（拼豆工艺后处理 A–G）由 Codex 实现并自测通过（108 例）。本文件给 Claude Code 做 **v1.1 终审**：真跑、逐项复核、修 P0/P1、产出审查报告。
> 前置必读：`README.md` → `docs/01 §8–10` → `docs/02 §9–11` → `docs/03 §9` → `docs/04 §7` → `docs/10`（任务书）→ `docs/11`（Codex v1.1 自测报告与偏差）。

## 1. 复现基线
- 工作目录 `D:\workspace\PixelBean`，git main（先确认用户已把 v1.1 提交推到 origin；以工作区与 origin 同步为佳）。勿改写历史。
- `npm test`（期望 108 全绿）、`npx tsc --noEmit`、`npm run build`（含 generate:palettes，3 套色板）、`npm run dev` + Playwright/Chrome 零 console 错误。

## 2. 复核矩阵
1. **DoD**（docs/01 §9）与 **T13–T20**（§10）：Codex 已在 docs/11 报告自测结果，你的任务是**独立复跑关键项**并抽查：T13（黄色渐变 golden ≤24 色/黄系 ≤3 层）、T15 噪点（深瞳孔保留）、T16 描边（空格不染、幂等）、T19（Hama 92/Perler 103 切换）、T20（116 底板无半格）；其余以报告+单测为准并标注。
2. **v1 不回归**：关闭全部后处理 + `maxColors:null` 的转换输出须与 v1 golden SHA 一致（复跑回归用例）。
3. **算法逐行对照 docs/03 §9**：
   - speckleClean（阈值/保护语义）、shadowSimplify（d* 锚点选取、彩色暗部保留）、clusterLimit（色相族规则与 docs 一致性，注意 docs/11 偏差记录"相邻 ≤20° 且族跨度 ≤60°"，确认与代码注释一致）、outline（两侧 1px 交界问题见 docs/11 残余风险，评估并给结论）。
   - **`xyzToLab` v1 bug 修复复查**：确认深色 Lab 数值正确且有回归测试（这是 v1 潜在历史 bug，复查换算是否影响既有 golden/图纸结果）。
   - 确定性：`src/core`（含 post/）无 `Math.random`；无 >400 行文件；post/ 零 DOM。
4. **数据红线**（docs/04 §7、docs/00 §1）：perler-standard/hama-midi 的 provenance（快照+LICENSE+sha256+crosscheck 差异表）齐全；无 AGPL/GPL/无证来源片段（git grep）；UI 对 community-legacy 有标注；不伪造官方全量。
5. **UI/交互**：目标模式下拉与参数联动"自定义"、K chips、暗部三档、噪点滑杆、描边开关、K+1 轮廓色提示、116 预设；移动端可用性不回归。

## 3. 修复与产出
- P0/P1 直接修复并补测试（一修一 commit）；P2 记入报告。
- 若用户已把真实角色图放入 `.tools/test-img/`：用"卡通/插画"模式做四项人工验收（黄系层数/暗部单层/孤立点/轮廓），对照图存 `.tools/test-img/`；若无图则如实说明，用内置样例复核。
- 产出 **`docs/13-阶段v1.1审查报告.md`**：环境验证 → DoD/T13–T20 复核表 → 代码审查逐项 → P0/P1 修复清单（commit）→ P2 → 偏差记录 → 残余风险；并更新 README 状态与索引。

## 4. 交接提示词模板（可直接整段复制）

```
你是 PixelBean v1.1（拼豆工艺后处理）的终审者（Claude Code）。
先完整阅读 README.md 与 docs/01 §8-10、02 §9-11、03 §9、04 §7、10、11。
工作目录 D:\workspace\PixelBean，git main 已有历史勿改写，新提交追加。

严格按 docs/12 执行：npm test(期望108)/tsc/build/dev+真实浏览器零报错；
独立复跑 T13-T16、T19-T20 与 v1 不回归用例；post/* 算法逐行对照 docs/03 §9
（重点：clusterLimit 族规则、outline 两侧1px、xyzToLab bug 修复复查）；
色板红线核查（provenance/无受限来源/UI 标注）；P0/P1 直接修复补测试；
若 .tools/test-img/ 有真实角色图则做四项人工验收对照。
产出 docs/13-阶段v1.1审查报告.md 并更新 README。
先给我 30 秒计划再开工，禁止只读代码不跑项目。
```
